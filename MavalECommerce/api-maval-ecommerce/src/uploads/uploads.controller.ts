import { Controller } from '@nestjs/common';

/**
 * Minimal uploads controller.
 * File uploads are handled through the PublicationsController.
 * This controller exists for potential future upload management endpoints.
 */
@Controller('uploads')
export class UploadsController {}
